# Chatlog

Template: Create React App

In the project directory, you can run:

### `yarn start && yarn dev`
`yarn start` = start the server
`yarn dev` = start the client server

### `yarn build`
Production build